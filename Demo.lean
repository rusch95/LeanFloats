import Demo.BatchInvariance

/-! # LeanFloats demos

A home for end-to-end demonstrations built on top of `IEEEFloat`
and `MX`.  Each demo lives in its own subdirectory with its own
headline theorem.

## Demos

  *  `Demo.BatchInvariance` — proof that a toy MXFP4 LLM is
     batch-invariant, forward and backward, bitwise.
-/
